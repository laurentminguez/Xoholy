{
  "name": "@xoholy/design",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "XOholy design tokens — the single source of truth for brand.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./tokens.css": "./dist/tokens.css"
  },
  "files": ["src", "dist"],
  "scripts": {
    "build": "node --experimental-strip-types scripts/build-css.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
